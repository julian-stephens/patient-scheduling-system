type: object
properties:
  id:
    $ref: ./UID.yaml
    nullable: true
  dateTime:
    type: string
    format: date-time
  reason:
    $ref: ./AppointmentAdmitReasons.yaml
  createdAt:
    $ref: ./CreatedAt.yaml
